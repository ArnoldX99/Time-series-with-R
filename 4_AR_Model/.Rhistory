Z.ts
z.ts
?ar
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z <- www[:,2]
Z <- www[;,2]
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
View(Z)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
www <- read.table("D://教学资料//研究生//时间序列//data//chapter11 us_rates.txt",head = TRUE)
Z <- www[2]
Z.ts <- ts(Z)
Z.ar <- ar(Z.ts)
mean(Z.ts)
Z.ar$order
Z.ar$ar
Z.ar$ar + c(-2, 2)
sqrt(Z.ar$asy.var)
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[-1,1])
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
Z.ar$ar
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[2])
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var)
acf(Z.ar$res[-1])
Z.ar$asy.var
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
View(Z)
sqrt(Z.ar$asy.var[1,1]
sqrt(Z.ar$asy.var[1,1]
sqrt(Z.ar$asy.var[1,1]
sqrt(Z.ar$asy.var[1,1]
Z.ar$asy.var[1,1]
c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-1])
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
www <- read.table("D://教学资料//研究生//时间序列//data//chapter11 us_rates.txt",head = TRUE)
Z <- www[2]
Z.ts <- ts(Z)
Z.ar <- ar(Z.ts)
mean(Z.ts)
Z.ar$order
Z.ar$ar
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
View(Z)
View(Z)
z.ts
Z.ts
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
www <- read.table("D://教学资料//研究生//时间序列//data//chapter11 us_rates.txt",header = TRUE)
Z <- www[2]
Z.ts <- ts(Z)
Z.ar <- ar(Z.ts)
mean(Z.ts)
Z.ar$order
Z.ar$ar
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-1])
acf(Z.ar$res)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
acf(Z.ar$res[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
acf(Z.ar$res[-1])
?acf
?`$,envRefClass-method`
View(Z.ar)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
acf(Z.ar$resid[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
www <- read.table("D://教学资料//研究生//时间序列//data//chapter11 us_rates.txt",head = TRUE)
Z <- www[2]
Z.ts <- ts(Z)
Z.ar <- ar(Z.ts)
mean(Z.ts)
Z.ar$order
Z.ar$ar
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$resid[-1])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
acf(Z.ar$res[-3])
#In the code above, a “−1” is used in the vector of
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
#In the code above, a “−1” is used in the vector of
#residuals to remove the first item from the residual
#series
#zˆt = 2.8 + 0.89(zt−1 − 2.8)
acf(Z.ar$res[-1])
source("D:/教学资料/研究生/时间序列/Chapter 4/aa.r")
Z.ar <- ar(Z.ts)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
View(Z)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
Z.ar$order
Z.ar$ar
Z.ar$ar[2] + c(-2, 2)*sqrt(Z.ar$asy.var[1,1])
acf(Z.ar$res[-3])
#In the code above, a “???1” is used in the vector of
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
source("D:/教学资料/研究生/时间序列/Chapter 4/Stock.R")
acf(Z.ar$res[-1])
#In the code above, a “???1” is used in the vector of
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
#In the code above, a “???1” is used in the vector of
#residuals to remove the first item from the residual
#series
#z_t = 2.8 + 0.89(z_t-1 - 2.8)
source("D:/教学资料/研究生/时间序列/Chapter 4/SimulationRand.R")
source("D:/教学资料/研究生/时间序列/Chapter 4/SimulationAR(1).R")
source("D:/教学资料/研究生/时间序列/Chapter 4/SimulationAR(1).R")
source("D:/教学资料/研究生/时间序列/Chapter 4/SimulationAR(1).R")
x.ar$ar[1] + c(-2,2) * sqrt(x.ar$asy.var[1,1])
plot(x, type = "l")
source("D:/教学资料/研究生/时间序列/Chapter 4/4.r")
source("D:/教学资料/研究生/时间序列/Chapter 4/4.r")
source("D:/教学资料/研究生/时间序列/Chapter 4/4.r")
#(d) Construct 95% confidence intervals for the parameter estimates of the fitted model.
#Do the model parameters fall within the confidence intervals? Explain your results.
x.ar$ar[1] + c(-2,2) * sqrt(x.ar$asy.var[1,1])
## [1] 0.7412990 0.8657963
x.ar$ar[2] + c(-2,2) * sqrt(x.ar$asy.var[2,2])
#(d) Construct 95% confidence intervals for the parameter estimates of the fitted model.
#Do the model parameters fall within the confidence intervals? Explain your results.
x.ar$ar[1] + c(-2,2) * sqrt(x.ar$asy.var[1,1])
x.ar$ar[2] + c(-2,2) * sqrt(x.ar$asy.var[2,2])
